syntax = "proto3";


package io.spring.api.raptor;


option (appId) = "raptor-realworld-java";

import "io/spring/api/raptor/Common.proto";


service PorfileApi{
	rpc favoriteArticle(PorfileReqeust) returns (ProfileResponse){
    	option (path) = "/profiles";
        option (method) = "get";
    };
    rpc followUser(PorfileReqeust) returns (ProfileResponse){
    	option (path) = "/profiles/follow";
        option (method) = "post";
    };
    rpc unFollowUser(PorfileReqeust) returns (ProfileResponse){
    	option (path) = "/profiles/follow/delete";
        option (method) = "post";
    };
}
 
 
message PorfileReqeust{
	string username = 1;
}
 
 
message ProfileResponse{
 	  Profile profile =1 ;
}